---
name: today
description: Start or continue today's daily log in ~/vault/daily/
---

Open today's daily log:

1. Compute today's date as YYYY-MM-DD.
2. Check if `~/vault/daily/<YYYY-MM-DD>.md` exists:
   - If it exists, read it and summarize what's already been logged today.
   - If it doesn't exist, create it with a header `# Daily Log — <YYYY-MM-DD>` and confirm it's ready for new entries.
3. Remind me of the vault protocol: keep sensitive personnel/performance info out of the daily log, but recommendations and decisions are fine.
4. Wait for me to tell you what to add.

Do NOT append to the log automatically. Wait for explicit instructions on what to record.
